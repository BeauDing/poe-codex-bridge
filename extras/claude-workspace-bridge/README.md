# Claude Workspace Bridge

This optional component contains the advanced Claude Code through Poe bridge implementation.

Public compatibility entrypoints remain in:

- `bin/claude-poe`
- `bin/claude-poe-review`

The actual advanced implementation lives here so the repository can stay API-first at the top level while preserving the older bridge workflow.
